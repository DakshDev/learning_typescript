// set types of the varaibles

// Privitive
let num: number = 2007;

let str: string = "helo world";

let bool: boolean = true;

let undef: undefined;

let union: string | null = null;
union = "hello world";

// Non-Primitive
let obj: object = {};

let arr: (number | string | boolean)[] = [1, 2, 3, "hello world", true];
